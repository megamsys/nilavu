$('#start_addon_id').empty();
$('#start_addon_id').html(' <input id="addon_id" name="addon_id" type="hidden" value="<%= @id %>" />');

$('#start_addon_name').empty();
$('#start_addon_name').html(' <input id="addon_name" name="addon_name" type="hidden" value="<%= @name %>" />');

$("#addon_start").modal({backdrop : false,keyboard: false,show : true});