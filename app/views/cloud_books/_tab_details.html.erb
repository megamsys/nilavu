				<div class="widget-box">
				<div class="widget-title">
				<span class="icon"> <i class="icon-th-list"></i> </span>
				<h5>Cloud Book</h5>
				</div>
				<div class="widget-content nopadding">
				<table class="table table-bordered table-striped table-hover">
				<thead>
				<tr>
				<th>Name</th>
				<th>Value</th>
				</tr>
				</thead>
				<tbody>

		<tr>
			<td><b>Node Name</b></td> <td> <%= cloud_book.node_name %></td>
		</tr>

		<tr>
			<td><b>Node Type</b></td> <td> <%= cloud_book.node_type %></td>
		</tr>
		<tr>

			<td><b>Platform</b></td> <td> <%= cloud_book.predefs[:name] %></td>
		</tr>
		<tr>
			<td><b>SCM</b></td> <td> <%= cloud_book.predefs[:scm] %></td>
		</tr>
<% if !cloud_book.predefs[:war].empty? %>
		<tr>
			<td><b>WAR</b></td> <td> <%= cloud_book.predefs[:war] %></td>
		</tr>
		<tr>
			<td><b>Status</b></td> <td> <%= cloud_book.status %></td>
		</tr>
<% end %>
				</tbody>

				</table>
				</div>
				</div>

				<div class="widget-box">
				<div class="widget-title">
				<span class="icon"> <i class="icon-th-list"></i> </span>
				<h5>Cloud Book Requests</h5>
				</div>
				<div class="widget-content nopadding">
				<table class="table table-bordered table-striped table-hover">
				<thead>
				<tr>
				<th>Actions</th>
				<th>Request Type</th>
				<th>Created At</th>
				<th>Show</th>
				<th>Destroy</th>

				</tr>
				</thead>
				<tbody>
				<% current_user.cloud_books.each do |cb| %>
				<tr>

<td>

    <div class="btn-group">
    <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
    Actions
    <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
    <li><%= link_to "Start", definition_path(cb.id), :target => "_self" %></li>
    <li><%= link_to "Stop", definition_path(cb.id), :remote => true %></li>
    <li><%= link_to "build", definition_path(cb.id), :remote => true %></li>
    <li><%= link_to "more", cloud_book_path(:id => cb.id, :action_name => "action"), :remote => true, data: { spinlock: '#book'} %></li>
    </ul>
    </div>

</td>
 				<td> <%= link_to "#{cb.name}", "http://#{cb.name}#{cb.domain_name}" , { :target => '_blank'} %> </td>
				<td> <%= cb.created_at.to_formatted_s(:rfc822) %> </td>

				<td> <%= link_to "View", cloud_book_path(cb.id), :remote => true, class: "btn btn-mini btn-success" %> </td>
				<td> <%= link_to "delete", cb, method: :delete, class: "btn btn-mini btn-success", :data => { :confirm => "Are you sure?"}%>
 </td>



				</tr>
				<% end %>
				</tbody>

				</table>
				</div>
				</div>

