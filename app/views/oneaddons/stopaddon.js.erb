$('#one_stop_addon_id').empty();
$('#one_stop_addon_id').html(' <input id="addon_id" name="addon_id" type="hidden" value="<%= @id %>" />');

$('#one_stop_addon_name').empty();
$('#one_stop_addon_name').html(' <input id="addon_name" name="addon_name" type="hidden" value="<%= @name %>" />');

$("#one_addon_stop").modal({backdrop : false,keyboard: false,show : true});