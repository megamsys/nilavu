<div class="row">
  <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 social">
    <ul class="list-inline text-center">
      <% Nilavu.authenticators.each do |authenticator| %>
        <li>
          <a href='auth/<%= "#{authenticator.name}" %>'  class='<%= "#{authenticator.name}" %> icon'></a>
        </li>
      <% end %>
    </ul>
  </div>
</div>
