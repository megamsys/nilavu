var selected_entity = '#setup_identity';

$(selected_entity).fadeIn("slow");

var to_insert_content = '<%= escape_javascript(render :partial => "cloud_identities/setup_identity", :locals => {:cloud_identity_launch => "successful" } ) %>';

insertAt(selected_entity,to_insert_content);

