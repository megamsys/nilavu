var selected_entity = '#federate_<%= @identity_type %>';

$(selected_entity).toggle();

$(selected_entity).fadeIn("slow");

var to_insert_content = '<%= escape_javascript(render :partial => "cloud_identities/postfederate" ) %>';
	
insertAt(selected_entity,to_insert_content);
