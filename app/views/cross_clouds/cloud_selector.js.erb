var selected_entity = '#provider_form';
var to_insert_content = "<%= escape_javascript(render :partial => 'cross_clouds/'+@provider_form_name+'_form', :locals => {:selected_cloud => @provider }) %>";
insertAt(selected_entity, to_insert_content);
