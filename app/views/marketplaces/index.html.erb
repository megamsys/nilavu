<% content_for :content_settings do %>
<% provide(:title, 'Marketplace') %>

			<%= render :partial => "marketplaces/category_view_form" , :locals => {:category => "All", :mkp_collection => @mkp_collection, :categories => @categories, :order => @order, :ssh_keys => @ssh_keys}%>
	
<% end %>
<%= render :template => users_show_path %>
