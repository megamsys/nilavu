<%= link_to "Add", apps_items_path(:product_id => product), class: "btn btn-primary", :method => :post, :remote => true, data: { spinner: '#user_form_spinner'}, :id => dom_id(product) %>

<%= dom_id(product) %>	

<%= spinner_tag 'user_form_spinner' %>
