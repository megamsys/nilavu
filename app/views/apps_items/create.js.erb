removeForm("#{#product_#{@product.id}}");
$("#product_#{@product.id}").fadeOut();

content = escape_javascript(render :partial => "apps_items/add_button", :locals => {:product => @product})
insertContent(".btn","#{content}");
