<%= link_to "Remove", apps_items_destroy_path(:id => apps_item_el.id,:product_id => apps_item_el.product_id), class: "btn btn-primary",  :remote => true,:method => :delete, data: { spinner: '#'+ dom_id(product) + '_user_form_spinner'} %>

<% product.description=1 %>

<%= spinner_tag dom_id(product) + '_user_form_spinner' %>
