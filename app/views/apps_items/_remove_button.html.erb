<%= link_to "Remove", apps_items_destroy_path(:id => apps_item_el.id), class: "btn btn-primary",  :remote => true,:method => :delete, data: { spinner: '#user_form_spinner'}, :id => dom_id(product) %>

<% product.description=1 %>	

<%= spinner_tag 'user_form_spinner' %>
