
var selected_entity = '#product_<%= @product.id %>';

$(selected_entity).toggle();

$(selected_entity).fadeIn("slow");

var to_insert_content = '<%= escape_javascript(render :partial => "apps_items/add_button", :locals => {:product => @product}) %>';
	
insertAt(selected_entity,to_insert_content);

  
