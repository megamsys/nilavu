{{#if showTop}}
    {{render "header"}}
{{/if}}

<div id="main-outlet">
    <div class="container">
        {{global-notice}}
    </div>
    {{outlet}}
    {{outlet "loading"}}
</div>

{{#if showFooter}}
    {{custom-html "footer"}}
{{/if}}

{{render "modal"}}
